<?xml version="1.0" encoding="utf-8"?>
<!--
  What FileProvider is allowed to hand to other apps.

  Deliberately broad on external storage: this is a file manager, and a share sheet that
  cannot share the file you are looking at is not a share sheet. It is still an explicit,
  per-URI, read-only grant - never a blanket permission.
-->
<paths>
    <external-path name="external" path="." />
    <external-files-path name="external_files" path="." />
    <cache-path name="cache" path="." />
    <files-path name="files" path="." />
    <root-path name="root" path="/" />
</paths>
